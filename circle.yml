## Customize the test machine
machine:
  node:
    version: 6.1.0

## Customize dependencies
dependencies:
  pre:
    - npm install
    - npm install coveralls # use a custom version of bundler
    - npm run test
    - npm run test:coveralls
